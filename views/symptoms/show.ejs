<h1><%= symptom.name %> - <%= symptom.severity %> out of 10!</h1>
<h3>
  <%= symptom.description %>
</h3>
<h2>Alright so... if you have this symptom, you may have these dieseases:</h2>
<ul class="list-group">
  <% symptoms.diseasess.forEach(function(s) { %>
    <li class="list-group-item-danger">
      <%= disease.name %>
    </li>
  <% }); %>
</ul>
